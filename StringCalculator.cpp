#include "StringCalculator.h"

int StringCalculator::add(string input){
        if(input == "") && (input == NULL)
                {
                        return 0;
                }
        return 1;
}
